/* global frontendApplication */

// create the controller and inject Angular's $scope
frontendApplication.controller('productController', function($rootScope, $scope, $routeParams, $http, AUTH_EVENTS, USER_ROLES, AuthService) {
    
});